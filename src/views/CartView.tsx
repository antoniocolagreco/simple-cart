import React from 'react';
import ViewContainer from '../components/UI/ViewContainer';
import css from './CartView.module.css';

interface Props {}

const CartView = (props: Props) => {
  return <ViewContainer>CART</ViewContainer>;
};

export default CartView;
